import { View } from 'react-native';

// This is a placeholder screen - the actual add button is handled in the tab bar
export default function Add() {
  return <View className="flex-1 bg-slate-50" />;
}
